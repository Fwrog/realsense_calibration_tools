# Data Directory

Local calibration captures are written here by default:

`data/calibration_images/d435i_color_charuco/`

This directory is intentionally kept out of public history. Captured images,
`.bag` recordings, and other raw sensor data may reveal scenes, locations, or
operators, so they are ignored by Git.
